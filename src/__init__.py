"""The gesture-recognition side of the project: dataset, training, evaluation and the app.

Importable as a package after `pip install -e . --no-deps`, so every module uses
`from src.<module> import ...` rather than editing `sys.path`.
"""
